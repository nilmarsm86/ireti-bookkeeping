export const onGoSubgenre = (dispatch) => {
    dispatch({ type: 'GO_SUBGENRE' });
};

export const onGoAccounting = (dispatch) => {
    dispatch({ type: 'GO_ACCOUNTING' });
};

export const onGoBook = (dispatch) => {
    dispatch({ type: 'GO_BOOK' });
};
